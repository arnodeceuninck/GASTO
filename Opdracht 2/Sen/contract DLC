// DLCItemType is het type van de elementen die in de queue
// worden opgeslagen.

+createDLC()
// Creëert een DLC queue.

+destroyDLC()
// Wist een DLC.

+isEmpty(): boolean {query}
// Bepaalt of een DLC leeg is.

+insert()
//bepaalt de index waar het element geplaatst moet worden in de DLC
//Shuift alle elementen vanaf de index waar het element komt 1 naar rechts.
//insert het element op de juiste index

+delete()
//bepaalt de index waar het element verwijderert moet worden in de DLC
//Verwijdert het element uit de DLC
//Shuift alle elementen vanaf de index+1 waar het element verwijdert werd 1 naar links.
